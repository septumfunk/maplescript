{
    "configurations": [
        {
            "type": "cppvsdbg",
            "request": "launch",
            "name": "Launch (Debug)",
            "preLaunchTask": "CMake: configure and build",

            "cwd": "${workspaceRoot}",
            "program": "${workspaceRoot}/out/debug/${fileWorkspaceFolderBasename}.exe"
        },
    ]
}